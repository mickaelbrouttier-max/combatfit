# Project Rules

- **Git updates**: Always stage, commit, and push modifications (`git add`, `git commit`, `git push`) to the remote repository immediately after making changes so that the live site is always updated.
